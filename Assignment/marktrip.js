tip = [];
bills = [77, 375, 110, 45];
function tipping(bill, tip) {
  this.bill = [];
  this.tip = [];
  this.percent = percent;
  this.bills.array.forEach(element => {
   
  });
  if (bill < 100) {
    percent = 0.2;
    tip = percent * bill;
  }
  else if(bill >= 100 && bill < 300){
   percent = 0.10
   tip = percent *bill
  }else(bill > 300){
   percent = 0.25
   tip = percent * bill
  }
}
